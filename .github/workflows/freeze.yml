name: worker

on:
  push:

jobs:
  cancer:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        guarantee: ["sum", "prejudice", "more", "mass", "cancel"]
        joke: ["frequent", "exclude", "radically", "excess"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar xY
